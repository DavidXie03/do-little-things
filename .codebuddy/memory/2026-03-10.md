# 2026-03-10

## UI/UX 改进修复（第二轮）
1. **卡片切换动画修复** - 改用 `v-show` + Vue `<transition name="card-fade-in">` + scoped CSS（opacity + scale + translateY），用 `requestAnimationFrame` 确保新卡片入场动画一定触发
2. **标题位置** - "做件小事" 改为卡片上方相对位置（`mb-8`），跟随卡片在页面垂直居中
3. **删除二次确认** - 新增 `confirmingDeleteId` 状态，第一次点击删除按钮变红（整个按钮背景变 #E17055、图标变白），3 秒内再次点击才真正删除
4. **弹窗圆角和间距** - 弹窗圆角从 `rounded-3xl`(24px) 改为 `rounded-2xl`(16px)，输入框圆角 `rounded-lg`(8px)，增减按钮圆角 `rounded-lg`，外 margin 24px
5. **操作按钮圆角** - "添加待办"/"重新生成" 按钮圆角从 `rounded-2xl` 改为 `rounded-xl`(12px)
修复了5个界面问题：
1. **卡片切换动画** - 使用 Vue `<transition>` + CSS `@keyframes cardSwipeIn` 实现平滑的缩放+上移入场动画，替代原来突兀的直接出现
2. **卡片滑动 bug** - 在 `TaskCard.vue` 的 `animateOut` 中添加了 `oncancel` 回调和 `setTimeout` 安全保险，确保 `swipe` 事件一定会被触发
3. **标题位置** - "做件小事" 标题从卡片正上方移到页面顶部固定位置（pt-16），与卡片保持更大间距
4. **今日待办删除按钮** - 在 `useStorage.ts` 添加 `removeTodoItem` 函数，`PendingView.vue` 每个待办项右侧添加了删除按钮（IconTrash）
5. **弹窗样式优化** - 添加待办弹窗增加了内边距（px-8 py-7）、外 margin（20px）、输入框内边距（px-5 py-3.5）、按钮高度（py-3.5）

## 代码重构
对项目进行全面重构，减少了 249 行代码（805 新增 / 1054 删除）：

### 删除无用文件
- 11 个未使用的图标组件（IconArrowDown, IconArrowLeft, IconArrowRight, IconChart, IconExplore, IconKnowledge, IconNews, IconSettings, IconSun, IconSwipe, IconTarget）
- 未使用的 `PendingItem.vue` 组件
- 未使用的 `mockTasks.json` 静态数据
- `dist/` 构建产物

### 清理冗余注释
- 删除了所有"一眼能看出"的注释：分隔线注释（`/* ===== xxx =====*/`）、变量/函数名即可说明的注释、HTML 模板中标记区域的注释

### 组件拆分
- 从 `PendingView.vue`（391 行）提取出 `TodoModal.vue`（添加/编辑弹窗）和 `TodoItem.vue`（单个待办行），PendingView 降至约 140 行

### Composable 拆分
- `useStorage.ts`（369 行）拆分为：
  - `storageCore.ts` - 底层存储（load/save/migrate/初始化）
  - `useRecords.ts` - 记录管理
  - `useCustomActions.ts` - 自定义待办 CRUD
  - `useDailyTodos.ts` - 每日待办管理
  - `useStorage.ts` - 统一导出入口（15 行）
- 从 `TaskCard.vue` 提取 `useSwipeGesture.ts`（手势逻辑 composable）
