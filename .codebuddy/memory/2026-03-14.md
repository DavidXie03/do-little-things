# 2026-03-14

## 设置页面和空状态视觉调整
- 作者名从 "David Xie" 改为 "DavidXie03"（zh.ts + en.ts）
- "关于"弹窗图标：从 ✨ emoji 替换为纯 SVG 层叠卡片 + 白色对勾设计（紫色渐变，参考主页卡片堆叠风格）
- 软件简介重写：强调"翻转卡片随机做事"带来的成就感
- "导入 / 导出" 重命名为 "备份与恢复" / "Backup & Restore"，图标从 ArrowDownUp 改为 Archive
- 待办列表空状态：文案改为"还没有任何待办"，图标从 IconParty 改为 Lucide ClipboardList，布局改为真正垂直居中（flex-1）
- Commit: `style: update about dialog, backup naming, and empty state design`

## 8项 UI 修复与功能增强
- **SVG 图标修正**: 关于弹窗 SVG 中浅色卡片从左上移到右下角（x=16,y=10）
- **Android 图标**: 替换 Capacitor 默认机器人图标为紫色层叠卡片+白色对勾 vector drawable，背景色 #F7F8FC
- **简介精简**: 去掉"收获更好的自己"/"become a better you"
- **全局 Toast 系统**: 新建 `useToast.ts` composable + `AppToast.vue` 组件，底部胶囊样式，支持 success/info/error 三种类型
- **完成任务表扬**: HomeView 滑动完成和 PendingView 列表点击完成时随机显示表扬文案
- **SettingsView 重构**: 删除局部 toast 改用全局 useToast
- **恢复 bug 修复**: importConfig 恢复后清除 dailyTodos 并调用 ensureDailyTodos() 重新生成今天待办
- **同天任务合框**: TodoItem 新增 grouped prop，PendingView 同天任务包裹在统一圆角框中
- **备份权限**: 安装 @capacitor/filesystem，创建 fileHelper.ts 封装权限检查和原生文件写入
- Commit: `feat: implement 8 UI fixes and feature enhancements`
