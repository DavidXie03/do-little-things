# 2026-03-16

## 清理无用 Android 图标文件
- 删除 5 个多余的 `ic_launcher_foreground.png`（mipmap-mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi）— 自适应图标使用的是 drawable/ 下的矢量 XML
- 删除重复的 `drawable-v24/ic_launcher_foreground.xml`（与 drawable/ 下的完全一致）
- 删除未被引用的 `drawable/ic_launcher_background.xml`（自适应图标引用的是 @color/ 而非 @drawable/）
- android/ 在 .gitignore 中，无需 git commit

## 修改 Android 图标卡片形状为长方形
- 原图标中卡片形状接近正方形（52:49），与关于页面 SVG 中的 36:44 比例不一致
- 精确按关于页面 SVG (viewBox 56x56) 等比缩放到 108dp 自适应图标画布
- 缩放因子 72/56 = 1.2857，偏移 18dp（安全区域边距）
- 修改文件：`android/app/src/main/res/drawable/ic_launcher_foreground.xml`

## 重新生成所有密度的 PNG 图标
- 之前只修改了矢量 XML（Android 8.0+ 使用），mipmap-* 下的 PNG（Android 7.x 使用）仍是旧的方形卡片
- 创建了 `scripts/gen_icons.py` 脚本，用 Pillow 将矢量定义渲染为 PNG
- 生成 10 个文件：5 个密度 × (ic_launcher.png + ic_launcher_round.png)
- 尺寸：mdpi=48, hdpi=72, xhdpi=96, xxhdpi=144, xxxhdpi=192
- 圆形图标使用超采样抗锯齿圆形蒙版

## 图标缩小 85% 留出边缘空隙
- 矢量 XML 用外层 `<group scaleX/Y="0.85">` 缩放
- PNG 生成脚本加 ICON_SCALE=0.85，所有坐标相对中心缩放

## App 改名：做件小事 → 小事一翻（Flip a Little）
- 中文名：做件小事 → 小事一翻
- 英文名：Do Little Things → Flip a Little
- kebab-case：do-little-things → flip-a-little（storage key、导出文件名等）
- appId (com.dolittlethings.app) 保持不变，避免被识别为不同 App
- 修改了 15 个文件共约 29 处引用
- 涉及：index.html, capacitor.config.ts, package.json, strings.xml, i18n/zh.ts, i18n/en.ts, SettingsView.vue, i18n/index.ts, useTheme.ts, storageCore.ts, useImportExport.ts, build-apk.sh, README.md, capacitor.config.json
