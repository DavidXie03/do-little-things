# 2026-03-12

## 代码重构完成

按照用户的5步重构标准对 do-little-things 项目执行了重构：

### Commit 1: 删除冗余注释
- 删除了 13 个文件中的分隔线注释（`/* ====== */`、`/* ----- */`）
- 删除了自解释的 JSDoc（如 `/** 格式化自定义日期显示 */`、`/** 将 Date 对象格式化为本地日期字符串 YYYY-MM-DD */`）
- 删除了模板区域标记注释（如 `<!-- 标签 -->`、`<!-- 当前语言 -->`、`<!-- 复选圆圈 -->`）
- 涉及文件：HomeView.vue, SettingsView.vue, style.css, types/index.ts, useTheme.ts, useDailyTodos.ts, useTabSwipe.ts, TodoModal.vue, TodoItem.vue, App.vue, storageCore.ts, taskService.ts

### Commit 2: 拆分过长文件
- 将 HomeView.vue（572行）中的卡片动画逻辑提取为 `src/composables/useCardAnimation.ts`
- 提取了 `playLeftSwipeAnimation`、`playRisingAnimation`、`playFlipAnimation` 等方法
- HomeView.vue 缩减至约 160 行（仅保留业务逻辑和模板）

### Commit 3: 删除无用文件和未使用的导出
- 删除了 4 个未使用的图标组件：IconAction.vue, IconDismiss.vue, IconHome.vue, IconRefresh.vue
- 删除了 types/index.ts 中未使用的：TaskTypeLabel, TaskTypeColor, ALL_TASK_TYPES, RecurrenceTypeLabel
- 清理了 useRecords.ts 中未使用的导出：completePendingTask, removePendingTask, pendingTasks, records, todayCompleteCount, getStreakDays
- 清理了 useCustomActions.ts 中未使用的导出：dailyConfig, updateDailyConfig, removeCustomAction, updateCustomActionRepeatCount
- 清理了 useDailyTodos.ts 中未使用的导出：dailyProgress, getNextUncompletedTodo, regenerateDailyTodos

### Commit 4: 清理未使用的导入和变量
- App.vue: 删除了未使用的 `prevIndex` 变量
- useTheme.ts: 删除了未使用的 `watch` 导入

## 移除左滑卡片返回效果

移除了左滑卡片后的翻面→背面→下沉→升起→翻回正面的复杂动画（left-flip → left-back → left-sink → rising → flipping），改为和右滑一样的简单飞出效果（向左飞出屏幕 → 新卡片升起翻面）。

修改文件：
- `useCardAnimation.ts`: 删除 `playLeftSwipeAnimation` 函数和 `left-flip`/`left-back`/`left-sink` 动画阶段，`triggerLeftSwipe` 改为和 `triggerRightSwipe` 相同逻辑
- `useSwipeGesture.ts`: 删除 `animateLeftSwipe` 函数和 `SwipeInfo` 接口，左滑改用 `animateOut` 飞出
- `HomeView.vue`: 移除 `left-flip`/`left-back`/`left-sink` 阶段的条件渲染，移除 `SwipeInfo` 依赖
- `TaskCard.vue`: 移除 `SwipeInfo` 导入，简化 emit 类型

## 禁止最后一个任务最后一次左滑

当今日只剩最后一个任务的最后一次时（`totalRemainingCount <= 1`），禁止左滑操作。实现方式：
- `useCardAnimation.ts`: 新增 `isLastRemaining` 计算属性
- `useSwipeGesture.ts`: 新增 `options.canSwipeLeft` 回调参数，在 `onTouchMove`/`onMouseMove` 中阻止向左拖拽
- `TaskCard.vue`: 新增 `disableLeft` prop，传给 `useSwipeGesture`
- `HomeView.vue`: 将 `isLastRemaining` 绑定到 `TaskCard` 的 `disable-left`

## 左滑阻尼效果

将禁止左滑时的"完全不可拖拽"改为"有阻尼的微弱拖拽"：
- 使用对数衰减阻尼函数 `dampedOffset`，最大偏移量限制为 30px（`LEFT_DAMPED_MAX`）
- 因为阻尼后偏移量永远达不到 `SWIPE_THRESHOLD_X`（100px），所以松手后会回弹，不会触发左滑
- 给用户"可以滑但是滑不动"的物理反馈感

## 修复删除未来待办不清理今天已完成项

问题：在未来待办列表中删除任务 A 后，今天已完成的任务 A 仍然显示在今天列表中。
原因：`removeTodoItem` 删除未来待办时只删除了 `customAction`，没有同步从 `dailyTodos.items` 中移除同一 `taskId` 的项。
修复：在分支 2（删除未来待办）中，删除 `customAction` 后增加过滤 `dailyTodos.items` 的逻辑，移除同一 `taskId` 的所有项（包括已完成的）。
