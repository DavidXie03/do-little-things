# 2026-03-17

## Fixed swipe indicator and vertical layout
- Removed external swipe-indicator from App.vue layout entirely
- Each view (CompletedView, PendingView) now uses full `scrollAreaHeight` instead of `scrollAreaHeight - 20`
- Added scrollable indicator inside PendingView scroll area (top) — scrolls with list content
- Added scrollable indicator inside CompletedView scroll area (bottom) — hint to pull up
- Simplified `verticalTranslateY`: index=1 → `-scrollAreaHeight`, index=0 → `0`; travel = `scrollAreaHeight`
- PendingView hidden (`visibility: hidden`) when CompletedView fully visible to prevent content leak
- Files modified: `src/App.vue`, `src/views/PendingView.vue`, `src/views/CompletedView.vue`, `src/composables/usePageSwipe.ts`
